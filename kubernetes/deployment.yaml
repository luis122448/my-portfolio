apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: my-portfolio
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-portfolio
  template:
    metadata:
      labels:
        app: my-portfolio
    spec:
      containers:
        - name: my-portfolio-container
          image: luis122448/my-portfolio:latest
          ports:
            - containerPort: 4000
